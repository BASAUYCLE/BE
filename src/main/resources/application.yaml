server:
  port: 8080
  servlet:
    context-path: /bike-platform

spring:
  datasource:
    url: jdbc:sqlserver://localhost:1433;databaseName=BikeShopDB;encrypt=true;trustServerCertificate=true
    username: sa
    password: Sa@12345
    driver-class-name: com.microsoft.sqlserver.jdbc.SQLServerDriver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.SQLServerDialect
        format_sql: true

# ImgBB API Configuration
imgbb:
  api-key: fcf7f7d6be47e140a51ba22996f3b228  # Get from https://api.imgbb.com/
  api-url: https://api.imgbb.com/1/upload
  expiration: 0  # 0 = no expiration, or set seconds (60-15552000)